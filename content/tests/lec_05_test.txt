1. В какой операционной системе устройства хранения поименованы латинскими буквами и эти имена используются при указании пути к файлу:
(x) Windows;
( ) Linux;
( ) Mac OS X.

2. Обычно имя файла состоит из двух компонентов:
(x) собственно имя и расширение, отделённое точкой;
( ) имя и дата создания файла;
( ) собственно имя и разрешение, отделённое точкой.

3. Относительный путь к файлу отсчитывается от:
( ) корневого директория (папки);
(x) текущего директория (папки);
( ) директория пользователя (папки).

4. Разделы носителя служат для:
( ) форматирование диска;
(x) организации на одном физическом устройстве нескольких независимых файловых систем;
( ) для объединения пространства хранения нескольких носителей в единую файловую систему.

5. Величина энтропии позволяет судить о:
( ) возможности сжатия данных с потерями;
( ) фрагментации данных на диске;
(x) теоретической границе, до которой можно сжать данные без потерь.

6. Удастся ли эффективно сжать jpeg-изображение архиватором:
( ) да, в несколько раз;
( ) да, но не более, чем в два раза;
(x) нет, изображение в формате jpeg не содержит большой избыточности (уже сжато).

7. Регулярную дефрагментацию следует проводить:
( ) для всех устройств хранения;
(x) только для дисков;
( ) современные файловые системы не требуют дефрагментации.


8. Технология RAID позволяет:
[x] объединять носители информации в один массив, который выглядит как диск;
[ ] прогнозировать время выхода диска из строя для своевременной замены;
[ ] повышать надежность хранения с помощью избыточности.

9. Протокол доступа к внешнему хранилищу NAS:
( ) SSD;
(x) SMB;
( ) DNS.

10. Укажи децентрализованную систему синхронизации файлов:
( ) Яндекс Диск;
(x) Syncthing;
( ) DropBox.



